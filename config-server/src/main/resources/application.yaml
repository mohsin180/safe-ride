spring:
  profiles:
    active: native
  application:
    name: config-server

  cloud:
    config:
      server:
        native:
          search-locations: file:///D:/projects/safe-ride/config-server/src/main/resources/config
server:
  port: 8888