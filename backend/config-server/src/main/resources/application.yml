spring:
  profiles:
    active: native
  application:
    name: config-server

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/
server:
  port: ${CONFIG_SERVER_PORT:8888}
logging:
  level:
    org.springframework.cloud.config: DEBUG