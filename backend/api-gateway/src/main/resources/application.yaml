spring:
  application:
    name: api-gateway
  main:
    web-application-type: reactive

#  cloud:
#    gateway:
#      server:
#        webflux:
#          routes:
#            - id: user-service
#              uri: http://localhost:8001
#              predicates:
#                - Path=/api/user/**
#            - id: profile-service
#              uri: http://localhost:8002
#              predicates:
#                - Path=/api/profile/**

server:
  port: ${GATEWAY_SERVER_PORT:8080}
  address: ${GATEWAY_ADDRESS:0.0.0.0}
jwt:
  # Must match user-services' jwt.secret (config-server: config/user-services.yml).
  # api-gateway does NOT pull from config-server, so keep these in sync manually.
  secret: ${JWT_SECRET:U2FmZVJpZGVTZWNyZXRLZXlGb3JKV1RUb2tlbkdlbmVyYXRpb24yMDI2U3ByaW5nQm9vdA==}
  expiration: ${JWT_EXPIRATION:86400000}
